[actor]
# name of the actor system
# defaults to hostname
name = "$HOSTNAME"
# amount of threads to run actors on
default_thread_pool_size = 0
# amount of threads to run system actors on
system_thread_pool_size = 0

